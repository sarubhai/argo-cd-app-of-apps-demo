{{- range .Values.clusters }}
{{- if .apps.enabled }}
{{- if .apps.memcached.enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{printf "%s-%s" .name "memcached"}}
  namespace: argo-cd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  project: k8s-app-resources
  source:
    repoURL: https://github.com/sarubhai/argo-cd-app-of-apps-demo
    targetRevision: {{ .apps.memcached.version }}
    path: argo-cd-applications/app-charts/memcached
    helm:
      releaseName: memcached
      valueFiles:
        - {{ .apps.valueFiles }}
      version: v3
  destination:
    name: {{ .name }}
    namespace: memcached
  syncPolicy:
    automated: 
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
      - ServerSideApply=true
      - RespectIgnoreDifferences=true
{{- end }}
{{- end }}
---
{{- end }}
